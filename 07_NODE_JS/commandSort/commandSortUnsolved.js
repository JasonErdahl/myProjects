// node commandSortUnsolved.js 2 5 3 8 1

var nodeArg = process.argv;
console.log(numArray.sort());
sortArray = [];
// Start with 2 to get all numeric values and add them to an array
for (var i = 2; i < nodeArg.length; i++) {
    sortArray.push(parseInt(nodeArg[i]));
}
console.log(numArray.sort());